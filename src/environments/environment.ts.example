// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
//
// Copy this file to environment.ts and fill in your values.
// DO NOT commit environment.ts — it is listed in .gitignore.

export const environment = {
  production: false,
  appVersion: '1.0.6',
  proURL: 'https://your-dev-server/',
  apiURL: 'https://your-dev-server/app/api/vims',
  portalApiURL: 'https://your-dev-server/app/api/portal',
  openAiApiKey: 'YOUR_OPENAI_API_KEY_HERE'
};
